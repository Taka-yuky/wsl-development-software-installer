# wsl-development-software-installer

1. GitHub CLIをインストール

```bash
sudo apt -y update
sudo apt -y install gh
```

2. GitHubへログイン
```bash
gh auth login
```

3. インストール用スクリプトを実行
```bash
cd wsl-development-software-installer
```
